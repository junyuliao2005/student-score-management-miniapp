# 自动化测试报告

更新日期：2026-07-11

## 环境与边界

- 后端测试数据库：SQLite 内存库。
- 外部 AI：真实 Key 元数据非空，本轮只做 1 次有界连接尝试并因网络受限 FAIL；离线总回归显式清空 Key，脚本显示 `SKIPPED`。
- OCR：MockOCR 流程测试；图片真实解码使用独立带 Pillow 的运行时验证。
- 未连接、初始化或清理真实 MySQL。

## 结果

| 检查 | 结果 |
|---|---|
| Python AST | 最终打包前全量检查 PASS |
| Mini Program JavaScript | 43/43 PASS |
| JSON | 38/38 PASS |
| pytest | 31/31 PASS |
| `verify_runtime.py` | 19/19 PASS |
| `verify_stats.py` | 31/31 PASS |
| `smoke_test_upload.py` | PASS |
| `test_ocr_pipeline.py` | PASS |
| DeepSeek real connection | FAIL，category=network；1 次、15 秒、0 重试 |
| DeepSeek offline error/fallback | 7/7 PASS |
| 关键路由注册 | 16/16 PASS（方法+路径总计 80） |
| 家长范围回归 | 6/6 PASS |
| YAML | 5/5 保守静态检查；未实际部署 |
| 标准库 trace 参考 | 4096/8353，可执行行 49.04%（非 coverage.py） |
| 高置信密钥扫描 | 最终打包时执行，真实 `.env` 永不读取入包 |

## 已覆盖风险

- 学生只能读取 published 成绩，撤回后不可见。
- 教师无绑定默认拒绝，班级和课程双重越权被拒绝。
- 家长绑定、已发布成绩和签名确认边界。
- CloudBase 临时 URL 域名、HTTPS、重定向、资源关闭。
- OCR 所有者隔离、过期和失败清理。
- Excel 预览所有者隔离和密码哈希不外泄。
- 导入预览零账本、确认原子性、异常回滚、安全撤销、人工修改保护和部分撤销。
- DeepSeek 仅 Chat Completions、最终内容解析、401/402/429/5xx/超时分类、有界重试和显式 fallback。
- 统计趋势/分布/进步/偏科的必填筛选、分页与教师范围。
- 学生 PDF published 边界、教师 XLSX 范围、家长确认汇总/导出。
- AI 历史角色可见性和反馈越权保护。

## 尚未验证

- 当前环境未安装 `coverage` / `pytest-cov`，未生成正式 HTML coverage；依赖已写入 `requirements-dev.txt`。49.04% 仅为标准库 trace 参考值。
- 使用独立 Python 3.12 的 ReportLab 真实生成 2901 字节 PDF，并由 pypdf 打开为 1 页；项目 `.venv` 需网络恢复后重装 requirements。
- 本地 MySQL 已先备份后执行 5 张表幂等迁移，并完成真实 HTTP 只读验收；未触碰云端 MySQL。
- coverage.py：SKIPPED，pytest-cov 因包索引受限未安装。
- 微信开发者工具 CLI 已发现但沙箱禁止写其用户数据目录；真机、云托管、真实 DeepSeek 成功响应和真实 RapidOCR 仍需人工验证。

## 本轮发现并修复

- `exam_publish_service.list_bindings()` 原本引用未初始化的 `teacher_classes`，会让家长绑定列表触发 `NameError`；现已按教师授权班级初始化并过滤。
- 确认列表中已计算教师班级范围但未使用；现已增加同一范围过滤，避免教师读取未授权班级的确认明细。
- 新增管理员全量/教师授权班级列表回归测试，并补充 DeepSeek 402/429/5xx/超时与 fallback；全量基线现为 31。
