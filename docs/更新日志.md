# 更新日志

## 2026-07-11

- 加固生产密钥、上传校验、临时缓存和 published 成绩隐私边界。
- 增加 DeepSeek Provider、结构化结果、Mock/Fallback 标识和无 Key 安全跳过。
- 增加 OCR 预览/人工修正/确认分析及 local/cloud 上传抽象。
- 增加教师班级/课程绑定、管理员维护页面及全链路范围校验。
- 增加 SQLite 隔离 pytest 和组合回归脚本。
- 增加原子导入批次账本、安全快照、管理员记录页面和非破坏性撤销。
- 增加按需趋势、分数段、进步榜、偏科分析和学生 published 趋势。
- 增加学生 PDF、教师成绩 XLSX、家长确认表 XLSX、待确认汇总。
- 增加 AI 历史页面、角色数据范围和反馈表。
- Docker 改为非 root，新增健康检查、Compose 和 Kubernetes 示例。
- 当前自动验证更新为 Python 113/113、JS 43/43、JSON 38/38、pytest 31/31、运行时 19/19。
- 修复家长绑定列表 `teacher_classes` 未初始化及确认列表教师班级范围未应用的问题，并增加回归测试。

云端等其他环境的新增表仍需人工备份后运行幂等迁移；本机数据库已完成备份与 5 张表迁移。

## 2026-07-11 本地真实集成候选版

- 新增拒绝非本地主机、隐藏密码的 `backup_local_mysql.py`；生成并校验本地单事务备份。
- 在本地 MySQL 创建 5 张缺失表并完成幂等复跑，未修改原成绩数据。
- 真实 Flask/MySQL HTTP 验收通过，密码登录因不读取凭据而人工保留。
- 依赖安装受网络限制；主回归仍通过，ReportLab/Pillow 使用独立 Python 3.12 验证。
- 增加仅 develop 可见的 local/cloud 一键开关，trial/release 强制 cloud；生成完整候选版和 GitHub 脱敏版并通过 CRC/密钥/隐私检查。
- DeepSeek REAL SKIPPED、OCR MOCK PASS / REAL SKIPPED，状态不混淆。
- 新增微信开发者工具、CloudBase、真机和预发布报告。
